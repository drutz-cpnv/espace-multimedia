<form method="post" class="form-button" action="{{ path('admin.type.delete', {'id': type.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer ce type d\'équipement ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.id) }}">
    <button class="small-btn-danger icon-btn" type="submit">{{ icon('delete') }}</button>
</form>