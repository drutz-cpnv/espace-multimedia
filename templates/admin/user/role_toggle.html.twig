{% extends 'admin_base.html.twig' %}

{% block body %}
    <div class="container">
        {% include 'admin/_admin-page_header.html.twig' with {
            icon: 'manage_accounts',
            title: 'Ajouter/retirer des rôles',
        } %}
        <div class="content">
            {{ form_start(form, {attr: {class: 'stacked'}}) }}
            <section class="stack-large">
                <section>
                    <div class="grid">
                        {{ form_row(form.users) }}
                        {{ form_row(form.role) }}
                    </div>
                </section>
                <section>
                    <button type="submit" class="btn-gradient">Enregistrer</button>
                </section>
            </section>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}